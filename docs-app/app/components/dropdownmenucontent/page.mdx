---
title: DropdownMenuContent
description: DropdownMenu Content Component
 
  Content container for dropdown menu.
  Wraps PopoverContent with menu-specific styling and role.
  Handles keyboard navigation (Arrow keys, Home/End, Escape).
---

import { APITable } from "@/components/docs/APITable";
import { LiveExample } from "@/components/docs/LiveExample";
import { DropdownMenuContentAPI } from "@/generated/api/dropdown-menu-content.api";

# DropdownMenuContent

DropdownMenu Content Component
 
  Content container for dropdown menu.
  Wraps PopoverContent with menu-specific styling and role.
  Handles keyboard navigation (Arrow keys, Home/End, Escape).

## Examples

<LiveExample>
{`import { DropdownMenuContent } from '@tenerife.music/ui';

export default function Example() {
  return <DropdownMenuContent>Example</DropdownMenuContent>;
}`}
</LiveExample>

## API

<APITable data={DropdownMenuContentAPI} />

## Props

No props available.
