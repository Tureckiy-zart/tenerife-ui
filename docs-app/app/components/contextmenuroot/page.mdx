---
title: ContextMenuRoot
description: ContextMenu Root Component
 
  Root component for ContextMenu that manages open/close state.
  Opens on right-click at cursor position.
---

import { APITable } from "@/components/docs/APITable";
import { LiveExample } from "@/components/docs/LiveExample";
import { ContextMenuRootAPI } from "@/generated/api/context-menu-root.api";

# ContextMenuRoot

ContextMenu Root Component
 
  Root component for ContextMenu that manages open/close state.
  Opens on right-click at cursor position.

## Examples

<LiveExample>
{`import { ContextMenuRoot } from '@tenerife.music/ui';

export default function Example() {
  return <ContextMenuRoot>Example</ContextMenuRoot>;
}`}
</LiveExample>

## API

<APITable data={ContextMenuRootAPI} />

## Props

No props available.
