---
title: PopoverArrow
description: Popover Arrow Component
 
  Arrow indicator pointing to the trigger element.
  Uses POPOVER_TOKENS for sizing and positioning.
---

import { APITable } from "@/components/docs/APITable";
import { LiveExample } from "@/components/docs/LiveExample";
import { PopoverArrowAPI } from "@/generated/api/popover-arrow.api";

# PopoverArrow

Popover Arrow Component
 
  Arrow indicator pointing to the trigger element.
  Uses POPOVER_TOKENS for sizing and positioning.

## Examples

<LiveExample>
{`import { PopoverArrow } from '@tenerife.music/ui';

export default function Example() {
  return <PopoverArrow>Example</PopoverArrow>;
}`}
</LiveExample>

## API

<APITable data={PopoverArrowAPI} />

## Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `size` | `"sm" | "md"` | No | md | Arrow size |

