---
title: PopoverTrigger
description: Popover Trigger Component
 
  Trigger element that opens/closes the popover.
  Supports asChild pattern for composition.
---

import { APITable } from "@/components/docs/APITable";
import { LiveExample } from "@/components/docs/LiveExample";
import { PopoverTriggerAPI } from "@/generated/api/popover-trigger.api";

# PopoverTrigger

Popover Trigger Component
 
  Trigger element that opens/closes the popover.
  Supports asChild pattern for composition.

## Examples

<LiveExample>
{`import { PopoverTrigger } from '@tenerife.music/ui';

export default function Example() {
  return <PopoverTrigger>Example</PopoverTrigger>;
}`}
</LiveExample>

## API

<APITable data={PopoverTriggerAPI} />

## Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `asChild` | `boolean` | No | false | Render as child element (composition pattern) |

