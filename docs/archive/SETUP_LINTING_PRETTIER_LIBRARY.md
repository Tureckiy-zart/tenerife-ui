# 📋 Задание: Настройка линтинга, Prettier и правил форматирования для библиотеки

## 🎯 Цель

Настроить идентичную конфигурацию ESLint, Prettier, TypeScript и Git hooks в другом репозитории (библиотеке), чтобы обеспечить единообразный стиль кода и автоматическое форматирование.

---

## 📦 Шаг 1: Установка зависимостей

### 1.1. Основные зависимости для devDependencies

Добавь в `package.json` следующие зависимости:

```json
{
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9.38.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react": "^7.37.2",
    "eslint-plugin-simple-import-sort": "^12.1.0",
    "eslint-plugin-unused-imports": "^4.1.4",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.7.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.18.1"
  }
}
```

### 1.2. Установка через pnpm

```bash
pnpm install --save-dev \
  @eslint/eslintrc@^3 \
  @types/node@^20 \
  @types/react@^19 \
  @types/react-dom@^19 \
  eslint@^9.38.0 \
  eslint-config-prettier@^9.1.0 \
  eslint-plugin-react@^7.37.2 \
  eslint-plugin-simple-import-sort@^12.1.0 \
  eslint-plugin-unused-imports@^4.1.4 \
  husky@^9.1.7 \
  lint-staged@^15.2.11 \
  prettier@^3.6.2 \
  prettier-plugin-tailwindcss@^0.7.1 \
  typescript@^5.9.3 \
  typescript-eslint@^8.18.1
```

---

## 📝 Шаг 2: Создание конфигурационных файлов

### 2.1. `.prettierrc` (в корне проекта)

Создай файл `.prettierrc` с содержимым:

```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2,
  "arrowParens": "always",
  "bracketSpacing": true,
  "jsxSingleQuote": false,
  "endOfLine": "auto",
  "useTabs": false,
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

### 2.2. `.prettierignore` (в корне проекта)

Создай файл `.prettierignore` с содержимым:

```
node_modules
.next
out
build
dist
coverage
public
*.mdx
*.svg
pnpm-lock.yaml
package-lock.json
```

### 2.3. `eslint.config.mjs` (базовая конфигурация для библиотеки)

Создай файл `eslint.config.mjs` в корне проекта:

```javascript
import prettierConfig from "eslint-config-prettier";
import reactPlugin from "eslint-plugin-react";
import simpleImportSort from "eslint-plugin-simple-import-sort";
import unusedImports from "eslint-plugin-unused-imports";
import tseslint from "typescript-eslint";

export default [
  {
    ignores: ["dist/**", "node_modules/**", "build/**", "coverage/**"],
  },
  {
    name: "base",
    files: ["**/*.{ts,tsx}"],
    plugins: {
      react: reactPlugin,
      "simple-import-sort": simpleImportSort,
      "unused-imports": unusedImports,
      "@typescript-eslint": tseslint.plugin,
    },
    languageOptions: {
      parser: tseslint.parser,
      parserOptions: {
        projectService: true,
        allowDefaultProject: true,
        ecmaVersion: "latest",
        sourceType: "module",
        ecmaFeatures: {
          jsx: true,
        },
      },
      globals: {
        React: "readonly",
        JSX: "readonly",
      },
    },
    settings: {
      react: {
        version: "detect",
      },
    },
    rules: {
      // ═══════════════════════════════════════════════════════════
      // REACT РАЗМЕТКА И JSX
      // ═══════════════════════════════════════════════════════════

      // Разрешаем апострофы и кавычки в тексте (не требуем &apos;)
      "react/no-unescaped-entities": "off",

      // ═══════════════════════════════════════════════════════════
      // TYPESCRIPT ТИПИЗАЦИЯ
      // ═══════════════════════════════════════════════════════════

      // any - отключено (временно, пока не исправим все типы)
      "@typescript-eslint/no-explicit-any": "off",

      // Неиспользуемые переменные - только предупреждение
      // Можно игнорировать переменные с префиксом _
      "@typescript-eslint/no-unused-vars": [
        "warn",
        {
          argsIgnorePattern: "^_", // Игнорировать аргументы: _arg
          varsIgnorePattern: "^_", // Игнорировать переменные: _var
          ignoreRestSiblings: true, // Игнорировать в деструктуризации
        },
      ],

      // Unused imports and variables (более строгий плагин)
      "unused-imports/no-unused-imports": "error",
      "unused-imports/no-unused-vars": [
        "error",
        {
          vars: "all",
          varsIgnorePattern: "^_",
          args: "after-used",
          argsIgnorePattern: "^_",
        },
      ],
      // Отключаем базовый TypeScript no-unused-vars в пользу unused-imports
      "@typescript-eslint/no-unused-vars": "off",

      // ═══════════════════════════════════════════════════════════
      // КОНСОЛЬ И ОТЛАДКА
      // ═══════════════════════════════════════════════════════════

      // console методы разрешены (log, info, debug, warn, error)
      // Только предупреждение для напоминания убрать из production
      "no-console": [
        "warn",
        {
          allow: ["log", "info", "debug", "warn", "error"],
        },
      ],

      // debugger в production - ошибка (нельзя оставлять в коде)
      "no-debugger": "error",

      // ═══════════════════════════════════════════════════════════
      // ПЕРЕМЕННЫЕ И КОНСТАНТЫ
      // ═══════════════════════════════════════════════════════════

      // const вместо let где возможно - предупреждение
      "prefer-const": "warn",

      // const вместо var - предупреждение
      "no-var": "warn",

      // Неиспользуемые параметры в функциях - предупреждение
      "no-unused-vars": "off", // Отключаем базовый, используем TypeScript версию

      // ═══════════════════════════════════════════════════════════
      // ИМПОРТЫ И ЭКСПОРТЫ
      // ═══════════════════════════════════════════════════════════

      // require() вместо import - ошибка (используем ES6 modules)
      "@typescript-eslint/no-require-imports": "error",

      // Анонимный дефолтный экспорт - предупреждение
      "import/no-anonymous-default-export": "warn",

      // Дублирующиеся импорты - предупреждение
      "no-duplicate-imports": "warn",

      // Import sorting (automatic)
      "simple-import-sort/imports": "warn",
      "simple-import-sort/exports": "warn",

      // ═══════════════════════════════════════════════════════════
      // КАЧЕСТВО КОДА
      // ═══════════════════════════════════════════════════════════

      // == вместо === - предупреждение (строгое сравнение рекомендуется)
      eqeqeq: ["warn", "always"],

      // Вложенные тернарные операторы - предупреждение
      "no-nested-ternary": "warn",

      // Несколько пустых строк подряд - предупреждение
      "no-multiple-empty-lines": ["warn", { max: 2, maxEOF: 1 }],

      // Пустые функции - предупреждение (лучше комментарий // todo)
      "no-empty-function": [
        "warn",
        {
          allow: ["arrowFunctions", "generatorFunctions"],
        },
      ],

      // Неэффективные циклы - предупреждение
      "no-loop-func": "warn",

      // Множественные пробелы - предупреждение
      "no-multi-spaces": "warn",

      // Неявные преобразования типов - отключено (!!value и +value допустимы)
      "no-implicit-coercion": "off",

      // Теневые переменные - отключено (TypeScript и так предупреждает)
      "no-shadow": "off",
      "@typescript-eslint/no-shadow": "off",

      // Неиспользуемые выражения - отключено (много false positives)
      "no-unused-expressions": "off",
      "@typescript-eslint/no-unused-expressions": "off",

      // Длинные функции - предупреждение (больше 150 строк)
      "max-lines-per-function": [
        "warn",
        {
          max: 150,
          skipBlankLines: true,
          skipComments: true,
        },
      ],

      // Сложность кода - предупреждение (цикломатическая сложность)
      complexity: ["warn", 20],

      // Максимальная глубина вложенности - предупреждение
      "max-depth": ["warn", 4],

      // Максимальное количество параметров - предупреждение
      "max-params": ["warn", 5],

      // ═══════════════════════════════════════════════════════════
      // СТИЛЬ И ЧИТАЕМОСТЬ
      // ═══════════════════════════════════════════════════════════

      // Предпочитать arrow functions
      "prefer-arrow-callback": "warn",

      // Использовать template strings вместо конкатенации
      "prefer-template": "warn",

      // Деструктуризация объектов где возможно
      "prefer-destructuring": [
        "warn",
        {
          array: false, // массивы можно не деструктурировать
          object: true, // объекты деструктурировать
        },
      ],

      // Спрятанные блоки под if - предупреждение
      "no-lonely-if": "warn",

      // ═══════════════════════════════════════════════════════════
      // БЕЗОПАСНОСТЬ
      // ═══════════════════════════════════════════════════════════

      // eval() - ошибка (опасно)
      "no-eval": "error",

      // ═══════════════════════════════════════════════════════════
      // REACT HOOKS
      // ═══════════════════════════════════════════════════════════

      // Отсутствующие зависимости в useEffect - ошибка
      "react-hooks/exhaustive-deps": "error",
      "react-hooks/rules-of-hooks": "error",

      // ═══════════════════════════════════════════════════════════
      // REACT СПЕЦИФИКА
      // ═══════════════════════════════════════════════════════════

      // Обязательные пропсы для React компонентов
      "react/prop-types": "off", // TypeScript заменяет prop-types
      "react/react-in-jsx-scope": "off", // React 17+ не требует import
      "react/jsx-key": "warn",
      "react/display-name": "warn",
    },
  },
  // Prettier integration (disables conflicting ESLint rules)
  prettierConfig,
];
```

### 2.4. `tsconfig.json` (базовая конфигурация)

Создай или обнови `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "node",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "allowSyntheticDefaultImports": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "baseUrl": ".",
    "declaration": true,
    "declarationMap": true,
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "build", "coverage"]
}
```

### 2.5. `lint-staged.config.js` (конфигурация для pre-commit)

Создай файл `lint-staged.config.js` в корне проекта:

```javascript
export default {
  "**/*.{ts,tsx,js,jsx}": ["prettier --write", "eslint --fix", "eslint --max-warnings=0"],
  "**/*.{json,md,css,html}": ["prettier --write"],
};
```

---

## 🔧 Шаг 3: Обновление package.json

### 3.1. Добавление скриптов

Добавь следующие скрипты в секцию `"scripts"` в `package.json`:

```json
{
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "lint:check": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "lint:strict": "eslint . --ext .ts,.tsx --max-warnings=0",
    "lint:errors": "eslint . --ext .ts,.tsx --format stylish",
    "precommit:lint": "eslint . --ext .ts,.tsx --fix",
    "precommit:format": "prettier . --write",
    "typecheck": "tsc --noEmit",
    "validate": "pnpm typecheck && pnpm lint:check && pnpm format:check"
  }
}
```

---

## 🪝 Шаг 4: Настройка Husky и Git Hooks

### 4.1. Инициализация Husky

```bash
# Инициализация Husky
pnpm exec husky init

# Создай pre-commit hook
pnpm exec husky add .husky/pre-commit
```

### 4.2. Содержимое `.husky/pre-commit`

Создай или обнови файл `.husky/pre-commit`:

```bash
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🛑 PRE-COMMIT: Running ESLint to show existing errors BEFORE autofix..."
pnpm lint:errors || true

echo "🧹 Running Prettier (format)..."
pnpm precommit:format

echo "🔧 Running ESLint autofix..."
pnpm precommit:lint

echo "🚫 Running ESLint strict mode (no warnings allowed)..."
pnpm lint:strict
```

### 4.3. Убедись, что файл исполняемый

```bash
chmod +x .husky/pre-commit
```

---

## ✅ Шаг 5: Проверка настройки

### 5.1. Запуск проверок

```bash
# Проверка форматирования
pnpm format:check

# Проверка линтинга
pnpm lint:check

# Проверка типов
pnpm typecheck

# Полная валидация
pnpm validate
```

### 5.2. Автоисправление перед коммитом

При попытке коммита Git hook автоматически:

1. ✅ Покажет существующие ошибки ESLint
2. ✅ Отформатирует код через Prettier
3. ✅ Применит автоисправления ESLint
4. ✅ Проверит код в строгом режиме (без предупреждений)

Если линтер найдет ошибки или предупреждения, коммит будет заблокирован.

---

## 📚 Шаг 6: Правила использования

### 6.1. Перед коммитом

Всегда запускай валидацию перед коммитом:

```bash
pnpm validate
```

Или доверься Git hook - он выполнит все проверки автоматически.

### 6.2. Ручное форматирование

```bash
# Форматировать все файлы
pnpm format

# Проверить форматирование без изменений
pnpm format:check
```

### 6.3. Ручной линтинг

```bash
# Проверить код и показать ошибки
pnpm lint:check

# Проверить и автоматически исправить
pnpm lint:fix

# Строгий режим (без предупреждений)
pnpm lint:strict
```

### 6.4. Проверка типов

```bash
pnpm typecheck
```

---

## 🔍 Шаг 7: Адаптация под библиотеку (опционально)

### 7.1. Если библиотека не использует React

Если в библиотеке нет React компонентов, можно упростить `eslint.config.mjs`:

- Удали секцию `react` из plugins
- Удали React-специфичные правила
- Удали `react-hooks` правила

### 7.2. Если библиотека использует только TypeScript (без JSX)

Обнови `eslint.config.mjs`:

```javascript
files: ["**/*.ts"], // убрать tsx
// Удалить все React правила
```

### 7.3. Кастомизация путей импорта

Если в библиотеке есть кастомные пути, обнови `tsconfig.json`:

```json
{
  "compilerOptions": {
    "paths": {
      "@library/*": ["./src/*"],
      "@utils/*": ["./src/utils/*"]
    }
  }
}
```

---

## 📋 Шаг 8: Чеклист завершения настройки

- [ ] Все зависимости установлены
- [ ] `.prettierrc` создан
- [ ] `.prettierignore` создан
- [ ] `eslint.config.mjs` создан и настроен
- [ ] `tsconfig.json` обновлен
- [ ] `lint-staged.config.js` создан
- [ ] Скрипты добавлены в `package.json`
- [ ] Husky инициализирован
- [ ] `.husky/pre-commit` создан и исполняемый
- [ ] `pnpm validate` проходит без ошибок
- [ ] Тестовый коммит проходит через pre-commit hook

---

## 🎯 Результат

После выполнения всех шагов:

1. ✅ Все файлы будут автоматически форматироваться через Prettier
2. ✅ ESLint будет проверять код на ошибки и предупреждения
3. ✅ Git hook будет блокировать коммиты с ошибками
4. ✅ Импорты будут автоматически сортироваться
5. ✅ Неиспользуемые импорты и переменные будут удаляться автоматически
6. ✅ Код будет соответствовать единому стилю проекта

---

## 📝 Дополнительные заметки

### Игнорирование переменных с префиксом `_`

Если переменная не используется, но нужна для совместимости типов, используй префикс `_`:

```typescript
// ✅ Правильно
const _unused = something;

// ❌ Неправильно - ESLint выдаст предупреждение
const unused = something;
```

### Автоматическая сортировка импортов

Плагин `eslint-plugin-simple-import-sort` автоматически сортирует импорты:

1. Внешние библиотеки
2. Внутренние модули
3. Типы

Не нужно сортировать вручную - ESLint сделает это автоматически при `--fix`.

### Prettier и ESLint интеграция

`eslint-config-prettier` отключает все ESLint правила, которые конфликтуют с Prettier, поэтому:

- Prettier отвечает за форматирование (отступы, кавычки, запятые)
- ESLint отвечает за качество кода (неиспользуемые переменные, ошибки)

---

**Дата создания:** 2025-01-29  
**Версия:** 1.0.0  
**Статус:** ✅ Готово к использованию
