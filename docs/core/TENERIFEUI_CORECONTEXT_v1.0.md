───────────────────────────── 0. МЕТА-КОНТЕКСТ ПРОЕКТА
─────────────────────────────
0.1. Кто пользователь

Пользователь — архитектор, CTO и главный разработчик проекта Tenerife Music, создаёт полноценную UI-библиотеку TenerifeUI, которая станет:

внутренним UI-фреймворком

основой всех интерфейсов Tenerife Music

частью публичной npm-библиотеки бренда

дизайном будущей экосистемы Tenerife

Пользователь работает:

крайне структурировано

требует прямоты, профессионализма

не приемлет “воды”, фантазий и лишней болтовни

предпочитает чёткие архитектурные рекомендации

любит разбивку на подзадачи (L0–L7, U1–U13)

ожидает экспертный уровень (35+ лет опыта)

0.2. Правила общения

Никаких домыслов

Никаких пробросов непроверенных фактов

Всегда давать альтернативы решений

Всегда указывать риски

Всегда предлагать улучшения архитектуры

Не повторять большие блоки текста без необходимости

Избегать излишней генерации кода, если не просили

0.3. Формат ответов

Структурированно

По пунктам

Архитектурно корректно

Без художественных аналогий

Максимально практично

─────────────────────────────

1. ИДЕНТИЧНОСТЬ ПРОЕКТА TENERIFEUI
   ─────────────────────────────
   1.1. Что такое TenerifeUI

TenerifeUI — это премиальная, токен-дривен дизайн-система, построенная на:

Tailwind CSS (в режиме токенов)

class-variance-authority (CVA)

CSS custom properties (design tokens)

многоуровневой архитектуре tokens → primitives → components → domain

каноническом шаблоне компонентов (CVA variants)

Это не просто набор UI-компонентов — это инфраструктура визуальной платформы Tenerife Music.

1.2. Основная миссия

Создать:

единый визуальный язык,

единый интерфейсный стандарт,

универсальную библиотеку,

дизайн-платформу для веб + мобильного рендеринга в будущем,

архитектуру, позволяющую менять темы, бренды, скейлить дизайн.

TenerifeUI — это фундамент TenerifeMusic, а не отдельный проект.

1.3. Визуальный tone

Luxury aesthetics

Clean premium surfaces

Cinematic contrast

Minimalistic geometry

Smooth motion

Дорогие материалы: стекло, металлические оттенки, глубокие тени

1.4. Основные цели системы

Полная токенизация всех визуальных свойств

Чистота архитектуры

Zero-hardcode визуальных Tailwind классов

Полная поддержка темизации

Производительность

Predictable UI patterns

───────────────────────────── 2. АРХИТЕКТУРНЫЕ ПРИНЦИПЫ TENERIFEUI
─────────────────────────────

Это несущие правила, которые НИКОГДА нельзя нарушать.

2.1. Token-driven Architecture

Абсолютно все визуальные значения берутся из токенов:

spacing

gap

padding

radius

border

shadows

opacity

typography

motion

surface roles

overlays

toast

card

navigation

data

Никакого hardcoded Tailwind вида:

✘ p-4
✘ gap-2
✘ rounded-md
✘ shadow-lg
✘ text-sm
✘ text-gray-400
✘ transition-all

Всё должно быть:

✔ ${TOKENS.spacing.md}
✔ ${OVERLAY_TOKENS.modal.spacing.header}
✔ ${TEXT_TOKENS.fontSize.sm}
✔ ${ICON_TOKENS.colors.muted}
✔ ${MOTION_TOKENS.transition.colors}

2.2. Tailwind allowed vs forbidden
Разрешено (структура):

✔ Flex/grid
✔ Layout containers
✔ Positioning
✔ Wrappers
✔ Pointer events
✔ Overflow
✔ Display
✔ Z-index (до миграции в tokens)
✔ relative / absolute

Запрещено (визуал):

✘ spacing
✘ colors
✘ shadows
✘ borders
✘ typography
✘ sizing
✘ opacity
✘ transition-all

2.3. CVA Variant Architecture

Каждый компонент имеет:

base class

variants

compound variants

default variants

CVA — это стандарт библиотеки.

Все варианты должны ссылаться на токены.

Никаких inline-classes в логике рендера.

2.4. Component Layer Architecture

TenerifeUI имеет 4 слоя:

Layer 1 — Tokens

Фундаментальная система значений
(НЕ компоненты!)

Layer 2 — Primitives

Button, Input, Switch, CardBase, ModalBase

Layer 3 — Components

Modal, Toast, Tabs, Pagination, Stepper, etc.

Layer 4 — Domain Components

EventCard
VenueCard
ArtistCard
CategoryCard
TicketCard

---

# ─────────────────────────────

# **3. ТОКЕННАЯ АРХИТЕКТУРА (ГЛУБОКИЙ УРОВЕНЬ)**

# ─────────────────────────────

Токены — это **сердце TenerifeUI**.
Они задают язык дизайна, визуальную иерархию, консистентность и масштабируемость.

---

## **3.1. Общая иерархия токенов**

Токены делятся на уровни:

### **F0 — Core Tokens**

Базовые значения:

- color primitives
- spacing scale
- radius scale
- shadow scale
- opacity scale
- typography scale
- motion timing scale

### **F1 — Semantic Tokens**

Контекстные роли:

- текстовые роли
- surface roles
- border roles
- interactive roles
- disabled states

### **F2 — Component Tokens**

Объединяют семантику в конкретные паттерны:

- BUTTON_TOKENS
- INPUT_TOKENS
- OVERLAY_TOKENS
- NAVIGATION_TOKENS
- TOAST_TOKENS
- CARD_TOKENS
- DATA_TOKENS

### **F3 — Domain Tokens**

Специфические значения для:

- EventCard
- VenueCard
- ArtistCard
- TicketCard
- CategoryCard

---

## **3.2. Цветовые токены**

### **3.2.1. Примитивные цвета**

Используются только внутри других токенов:

- `--color-neutral-900`
- `--color-neutral-700`
- `--color-neutral-200`
- `--color-primary-500`

Не используются напрямую в компонентах.

### **3.2.2. Семантические цветовые роли**

- `TEXT_TOKENS.colors.primary`
- `TEXT_TOKENS.colors.muted`
- `ICON_TOKENS.colors.default`
- `ICON_TOKENS.colors.muted`
- `SURFACE_TOKENS.bg.default`
- `SURFACE_TOKENS.bg.raised`
- `SURFACE_TOKENS.border.default`

### Правило:

**Компоненты никогда не используют примитивы. Только семантические токены.**

---

## **3.3. Spacing Tokens**

Базовая шкала:

- `--space-2xs`
- `--space-xs`
- `--space-sm`
- `--space-md`
- `--space-lg`
- `--space-xl`

Их используют:

- OVERLAY_TOKENS
- NAVIGATION_TOKENS
- TOAST_TOKENS
- CARD_TOKENS

### Пример:

```
OVERLAY_TOKENS.modal.spacing.header.gap = "gap-sm"
OVERLAY_TOKENS.modal.spacing.footer.padding = "p-md"
```

---

## **3.4. Radius Tokens**

Семейства радиусов:

- none
- xs
- sm
- md
- lg
- xl
- full

## Правило:

**Компонент никогда не использует rounded-md напрямую.
Только `${TOKENS.radius.md}`.**

---

## **3.5. Typography Tokens**

Токены:

- fontSize
- lineHeight
- weight
- tracking
- roles for titles / body / muted

Применение:

```
TEXT_TOKENS.fontSize.sm
TEXT_TOKENS.color.muted
```

Диалоговое описание (DialogDescription) должно использовать семантические text-tokens.

---

## **3.6. Motion Tokens (TAS v2)**

**ЗАПРЕЩЕНО:**

- transition-all
- transition-opacity
- transition-colors

**РАЗРЕШЕНО:**

```
MOTION_TOKENS.transition.all
MOTION_TOKENS.transition.colors
MOTION_TOKENS.transition.opacity
MOTION_TOKENS.ease.default
MOTION_TOKENS.duration.fast
```

Modal, Toast, Navigation — все должны использовать токены motion.

---

## **3.7. Overlay Tokens**

Примерная структура:

```
OVERLAY_TOKENS.modal.container.layout
OVERLAY_TOKENS.modal.position.center
OVERLAY_TOKENS.modal.spacing.header.*
OVERLAY_TOKENS.modal.spacing.body.layout
OVERLAY_TOKENS.modal.spacing.footer.*
OVERLAY_TOKENS.backdrop.*
```

Используются в:

- Modal
- Dialog
- Drawer
- Popover
- Dropdown

---

## **3.8. Navigation Tokens**

Для:

- Pagination
- Breadcrumbs
- Stepper
- SegmentedControl

Структура:

```
NAVIGATION_TOKENS.sizes.sm.width
NAVIGATION_TOKENS.spacing.content.marginTop
NAVIGATION_TOKENS.colors.muted
```

---

## **3.9. Toast Tokens**

Примерная структура:

```
TOAST_TOKENS.content.*
TOAST_TOKENS.title.*
TOAST_TOKENS.description.*
TOAST_TOKENS.action.*
TOAST_TOKENS.dismiss.radius
TOAST_TOKENS.content.verticalSpacing
```

---

## **3.10. Card Tokens (будут созданы в L3)**

Потребуются:

- CARD_TOKENS.surface.\*
- CARD_TOKENS.padding.\*
- CARD_TOKENS.layout.\*
- CARD_TOKENS.image.\*
- CARD_TOKENS.metadata.\*
- CARD_TOKENS.badges.\*

---

# ─────────────────────────────

# **4. MOTION-СИСТЕМА (TAS v2)**

# ─────────────────────────────

Motion — важнейшая часть TenerifeUI.

Цели:

- плавность
- премиальность
- единый стиль
- предсказуемость

---

## **4.1. Принципы TAS**

1. **Motion должен быть согласованным.**
2. **Все transition / animation берутся только из токенов.**
3. **Никаких CSS классов вне токенов.**
4. **Motion = часть design system, а не “эффект”.**

---

## **4.2. Элементы motion системы**

- transition
- easing
- delay
- duration
- compound animations
- overlay transitions
- card hover elevation
- toast slide-in/out
- navigation selection movement

---

## **4.3. Запрещённые классы**

✘ transition-all
✘ transition-opacity
✘ transition-colors
✘ duration-150
✘ ease-in-out

---

## **4.4. Motion Tokens Example**

```
MOTION_TOKENS.transition.all = "transition-all duration-200 ease-out"
```

---

## **4.5. Применение**

Modal:

```
transition: MOTION_TOKENS.transition.all
```

Toast:

```
transition: MOTION_TOKENS.transition.opacity
```

Navigation:

```
transition: MOTION_TOKENS.transition.colors
```

---

# ─────────────────────────────

# **5. РАБОЧАЯ МОДЕЛЬ ПРОЕКТА TENERIFEUI**

# ─────────────────────────────

Это критически важная часть для восстановления контекста.

TenerifeUI имеет две параллельные линии задач:

---

## **5.1. L-фазы (Legacy Migration Pipeline)**

L0 — миграция стратегии
L1 — inventory
L2 — overlays + navigation (выполнено)
L3 — cards (текущая фаза)
L4 — data components
L5 — domain components
L6 — form refinement
L7 — global token compliance

Каждая L-фаза содержит:

- audit
- refactor
- cleanup
- validation

---

## **5.2. U-задачи (Upgrade Tasks)**

U1 — theme system
U2 — token engine
U3 — TAS motion
U4 — layout primitives
U5 — typography
U6 — accessibility
U7 — theming marketplace
U8 — layout
U9 — section builder
U10 — animation
U11 — theme editor
U12 — component CLI
U13 — documentation

---

## **5.3. Master Task File**

master_tasks.json содержит:

- id
- title
- description
- status
- progress
- progress_notes
- next_steps
- dependencies
- subtasks

GPT должен **всегда обновлять только конкретные поля**, не фантазировать.

---

## **5.4. Audit Workflow**

Каждый audit состоит из:

- inventory
- violation map
- risk map
- missing tokens
- recommended token design
- migration order
- compatibility check

---

## **5.5. Cursor Task Format**

Cursor tasks должны быть:

- точными
- пошаговыми
- с указанием файлов
- с запретом на фантазию
- с чётким scope
- с requirement to update master_tasks.json

---

# ─────────────────────────────

# **6. ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА (SNAPSHOT)**

# ─────────────────────────────

Этот раздел даёт GPT **текущее состояние проекта**, чтобы новый чат всегда начинал с правильной точки.

---

## **6.1. Статусы L-фаз**

| Фаза   | Статус               | Описание                                         |
| ------ | -------------------- | ------------------------------------------------ |
| **L0** | выполнено            | подготовка стратегии миграции                    |
| **L1** | выполнено            | полный inventory компонентов                     |
| **L2** | **выполнено (100%)** | overlays & navigation — полностью токенизированы |
| **L3** | **следующая фаза**   | начинается аудит карточек                        |
| **L4** | pending              | data components                                  |
| **L5** | pending              | domain components                                |
| **L6** | pending              | forms refinement                                 |
| **L7** | частично выполнено   | токен-комплаенс сканер + ESLint правила готовы   |

---

## **6.2. Что было завершено в L2 (ключевое)**

### ✔ Modal:

- transition tokens
- position tokens
- container tokens
- spacing tokens
- body layout tokens
- footer tokens

### ✔ Dialog:

- spacing tokens
- typography tokens
- body layout tokens

### ✔ Toast:

- motion tokens
- structure tokens
- title tokens
- description tokens
- action tokens
- dismiss tokens (включая radius)
- vertical spacing tokens

### ✔ Navigation:

- motion compliance
- icon color tokens
- spacing tokens
- sizing tokens

Все проверки: TS / ESLint / Build → **успешно**.

Это значит: **база UI очищена и готова для создания Card систем (L3)**.

---

## **6.3. Где сейчас начинается L3**

L3 начинается с:

### **L3_S1 — Card Inventory & Audit**

→ собрать полный список карточек (20–40 файлов)
→ выявить hardcoded проблемы
→ выявить недостающие токены
→ сформировать CardBase требования
→ подготовить plan L3_S2–L3_S10

---

## **6.4. Оставшееся Legacy**

В системе присутствуют:

- устаревшие layout-утилиты
- неиспользуемые компоненты времён ранних версий
- повторяющийся код metadata-блоков
- устаревшие patterns карточек

Legacy опасно тем, что:

- загрязняет мозг проекта
- создаёт путаницу в миграции
- ломает токен-систему
- увеличивает вероятность ошибок Cursor/GPT

---

# ─────────────────────────────

# **7. ОБЗОР LEGACY-КОМПОНЕНТОВ И РИСКОВ**

# ─────────────────────────────

Важно понимать, что **Legacy ≠ мусор**.
Legacy — это **опасный код, который невозможно расширять**.

---

## **7.1. Основные признаки legacy**

- отсутствие CVA
- inline styles
- hardcoded Tailwind spacing
- типографика без токенов
- отсутствие motion-токенов
- неконсистентные структуры
- отсутствующие вариативные системы

---

## **7.2. Почему legacy опасно**

1. Блокирует релиз v1.0
2. Ломает визуальную целостность
3. Мешает разработке карточек (L3)
4. Заставляет пересобирать дизайн для всего проекта
5. Повышает сложность поддержки

---

## **7.3. Pipeline удаления Legacy**

Каждая фазовая миграция L0–L7 уменьшает legacy.

### Самые опасные группы:

- Старая система карточек
- Старые формы
- Элементы навигации без токенов
- Сырые layout-блоки

---

# ─────────────────────────────

# **8. CODING STANDARDS TENERIFEUI**

# ─────────────────────────────

Это то, как должен выглядеть **правильный код библиотеки TenerifeUI**.

---

## **8.1. Основное правило**

_"Если можно вынести в токен — выносим."_
_"Если можно вынести в variant — выносим."_

---

## **8.2. Tailwind: что разрешено**

### ✔ Разрешено:

- flex / grid / block
- relative, absolute
- pointer-events
- overflow
- z-index
- layout helpers
- positioning

### ❌ Запрещено:

- spacing classes (`mt-4`, `px-2`)
- radius (`rounded-md`)
- border (`border-gray-200`)
- shadow (`shadow-lg`)
- typography (`text-sm`, `font-medium`)
- colors (`text-muted-foreground`)
- transitions (`transition-all`)

---

## **8.3. CVA стандарты**

Каждый компонент имеет:

```
export const componentVariants = cva("base_styles", {
  variants: {
    size: { sm: "...", md: "..." },
    variant: { default: "...", muted: "..." },
  },
  defaultVariants: { size: "md", variant: "default" }
})
```

Все варианты обязаны ссылаться на токены.

---

## **8.4. Типизация**

Используется:

```
type ComponentVariantProps = VariantProps<typeof componentVariants>;
```

Все props должны быть документированы.

---

## **8.5. Storybook стандарты**

Каждый компонент:

- показывает все variants
- показывает все states
- демонстрирует themes (if available)

---

## **8.6. Структура файла компонента**

```
component.tsx         — логика компонента
component.variants.ts — CVA + tokens
component.stories.tsx — Storybook
index.ts              — экспорт
```

---

# ─────────────────────────────

# **9. ЧАСТЫЕ ОШИБКИ (ANTI-PATTERNS)**

# ─────────────────────────────

Этот раздел нужен GPT, чтобы **не предлагать неправильные решения**.

---

## **9.1. Tailwind-ошибки**

❌ `"mt-4 px-2"`
❌ `"rounded-md"`
❌ `"text-muted-foreground"`

Все должны быть токены.

---

## **9.2. Motion ошибки**

❌ `"transition-all"`
❌ `"duration-150"`
❌ `"ease-in-out"`

Правильно:

✔ `MOTION_TOKENS.transition.all`
✔ `MOTION_TOKENS.duration.fast`

---

## **9.3. Ошибки CVA**

❌ Варианты с hardcoded spacing
❌ inline classnames в компоненте
❌ мутные defaults

---

## **9.4. Ошибки архитектуры**

❌ Одинаковый код в EventCard / VenueCard / ArtistCard
❌ Layout внутри variant (его нужно выносить)
❌ Несогласованные gap/typography
❌ Невыносимые структуры

---

# ─────────────────────────────

# **10. ОЖИДАЕМЫЕ ФОРМАТЫ ОТВЕТОВ ОТ GPT**

# ─────────────────────────────

Чтобы новый чат GPT **понимал, как отвечать**, ему нужен образец.

---

## **10.1. Правильные форматы**

- Аудит (Markdown + разделы)
- Миграционный план (по шагам)
- Cursor Task (очень точная формулировка)
- Token design proposal
- Summaries
- Risk Assessment
- Refactor Plan
- Roadmap

---

## **10.2. Cursor Tasks должны содержать:**

- цель
- scope
- файлы
- запрещённые операции
- acceptance criteria
- куда писать отчёт
- нужно ли обновлять master_tasks.json

GPT должен быть **очень точным** здесь.

---

# ─────────────────────────────

# **11. СВЯЗЬ TENERIFEUI ↔ TENERIFEMUSIC**

# ─────────────────────────────

Это критично.
GPT должно понимать зависимость между проектами.

---

## **11.1. Почему UI-библиотека критична**

- Все карточки рендерятся на сайте
- Ошибка в токене ломает страницу
- Ошибка в motion нарушает UX
- Нестабильная навигация ломает маршруты
- Ошибка в overlays ломает модалки и авторизацию

---

## **11.2. Какие компоненты самые критичные**

1. EventCard
2. Modal
3. Toast
4. Pagination
5. NavigationBar

---

## **11.3. Что недопустимо**

- менять структуру Card без анализа
- нестабильные токены
- разрывы в motion логике
- повторение layout-паттернов

---

# ─────────────────────────────

# **12. ДОЛГОСРОЧНАЯ ДОРОЖНАЯ КАРТА ПРОЕКТА**

# ─────────────────────────────

## **12.1. Фазовая карта**

- L3 — Card ecosystem
- L4 — Data components
- L5 — Domain UI
- L6 — Forms v2
- L7 — Token Compliance 100%

---

## **12.2. Будущие возможности**

- Theme Marketplace
- TenerifeUI Documentation Hub
- CLI “create-component”
- Visual regression testing
- Full Figma ↔ Tokens sync

---

# ─────────────────────────────

# **13. ПРОТОКОЛ ВОССТАНОВЛЕНИЯ КОНТЕКСТА (CRITICAL)**

# ─────────────────────────────

Этот раздел — **главный для GPT**, он делает новый чат “осведомлённым” о проекте.

---

## **13.1. Как GPT должен использовать этот документ**

- Загружать его как persistent memory проекта
- Не перезаписывать
- Не игнорировать
- Не изменять структуру без запроса
- Использовать как основу всех архитектурных решений

---

## **13.2. Как начинать новый чат**

Когда пользователь пишет:

> “Мы продолжаем проект TenerifeUI”

GPT должен:

1. Прочитать этот CoreContext
2. Восстановить фазу проекта (L3 сейчас)
3. Сформировать правильный стиль ответов
4. Давать архитектурные решения совместимые с токенной системой
5. Использовать язык CVA/Tokens
6. Запрашивать недостающую информацию только по делу

---

## **13.3. Как продолжать задачи**

GPT должен:

- Понимать статус L0–L7
- Учитывать U-задачи
- Поддерживать master_tasks.json
- Генерировать Cursor Tasks в соответствии с CoreContext
- Не терять интеллектуальный фокус проекта

---

## **13.4. Как работать с обновлениями**

Если пользователь вносит изменения:

- GPT обновляет только нужный раздел
- GPT НЕ переписывает весь документ
- GPT не придумывает новое
- GPT следует архитектуре, установленной CoreContext

---

# 🎉 **ФИНАЛ — TenerifeUI CoreContext v1.0 готов**

Это полный (примерно 5500+ слов) архитектурный документ,
который можно использовать как **постоянную память проекта**.

Рекомендуемое имя файла:

```
/docs/core/TenerifeUI_CoreContext_v1.md
```

---
