# 🔍 Аудит хардкоженных токенов

**Дата создания:** 2025-12-09  
**Цель:** Найти все места, где используются хардкоженные значения вместо системы токенов Tenerife UI

---

## 📋 Резюме

В проекте найдено множество мест, где используются хардкоженные значения вместо токенов. Основные категории проблем:

1. **Хардкоженные hex цвета** в Storybook stories
2. **Числовые Tailwind классы** (px-4, py-2, gap-8 и т.д.) вместо семантических токенов
3. **Прямое использование CSS переменных** в классах вместо токенов
4. **Inline стили** с хардкоженными значениями
5. **Числовые размеры** (h-4, w-6, rounded-lg) вместо токенов

---

## 🎨 Категория 1: Хардкоженные цвета

### 1.1 Hex цвета в Storybook stories

**Файл:** `src/components/popover/Popover.stories.tsx`

```typescript
// Строки 392-393
{ name: "light", value: "#ffffff" },
{ name: "dark", value: "#0a0a0a" },
```

**Проблема:** Используются хардкоженные hex цвета вместо токенов из системы.

**Решение:** Использовать токены:

```typescript
import { colors } from "@/tokens";
// или
{ name: "light", value: "hsl(var(--background))" },
{ name: "dark", value: "hsl(var(--background))" },
```

---

## 📏 Категория 2: Числовые Tailwind классы вместо токенов

### 2.1 Spacing (отступы)

Найдено множество использований числовых классов для отступов:

**Примеры:**

1. **`src/components/SectionBuilder.stories.tsx`**
   - Строка 452: `space-y-8 p-8` → должно быть `space-y-lg p-lg` или токены
   - Строка 454: `mb-4` → должно быть `mb-md`
   - Строка 457: `p-4` → должно быть `p-md`
   - Строка 489: `mt-8` → должно быть `mt-lg`

2. **`src/components/toasts/ToastProvider.tsx`**
   - Строки 43-48: `top-4`, `left-4`, `right-4`, `bottom-4` → должны использовать токены spacing

3. **`src/components/feedback/ConsentBanner.tsx`**
   - Строка 29: `mb-2` → должно быть `mb-sm` или токен

4. **`src/components/filters/PriceRangeSlider.tsx`**
   - Строка 151: `left-3`, `top-1/2` → `left-sm`, токены для позиционирования
   - Строка 164: `pl-8` → `pl-lg`

5. **`src/components/filters/SearchInput.tsx`**
   - Строка 67: `left-3` → `left-sm`
   - Строка 76: `pl-10 pr-10` → `pl-xl pr-xl` или токены
   - Строка 87: `h-6 w-6` → токены размера иконки

6. **`src/components/filters/DateRangePicker.tsx`**
   - Строка 96: `mr-sm` (правильно)
   - Строка 125: `h-8 w-8` → токены размера

### 2.2 Размеры (width/height)

**Примеры:**

1. **`src/components/icons/TrendingIcon.tsx`**
   - Строка 14: `h-6 w-6` → должно использовать токены размера иконки

2. **`src/components/cards/VenueCard.tsx`**
   - Строка 110: `h-16 w-16` → токены размера
   - Строка 132: `text-lg` → токен типографии
   - Строка 151: `h-4 w-4` → токены размера иконки

3. **`src/components/cards/EventCard.tsx`**
   - Строка 82: `right-3 top-3` → токены spacing
   - Строка 100: `h-16 w-16` → токены размера
   - Строка 122: `text-lg` → токен типографии
   - Строка 141: `h-4 w-4` → токены размера иконки

4. **`src/components/data/Timeline.tsx`**
   - Строка 27: `h-3 w-3` → токены размера
   - Строка 28: `h-12 w-px` → токены spacing и размера

5. **`src/components/feedback/Progress.tsx`**
   - Строка 17: `h-2 w-full` → токены размера
   - Строка 19: `h-2` → токен размера

6. **`src/components/skeletons/VenueCardSkeleton.tsx`**
   - Строка 15: `h-48` → токен размера
   - Строка 16: `h-6` → токен размера
   - Строка 17: `h-4` → токен размера

7. **`src/components/skeletons/EventCardSkeleton.tsx`**
   - Аналогичные проблемы

### 2.3 Border Radius

**Примеры:**

1. **`src/components/SectionBuilder.presets.tsx`**
   - Строка 203: `rounded-lg` → токен radius
   - Строка 382: `rounded-full` → может быть OK, но лучше проверить

2. **`src/components/SectionBuilder.stories.tsx`**
   - Строка 147: `rounded-lg` → токен radius
   - Строка 194: `rounded-lg` → токен radius
   - Строка 457: `rounded-lg` → токен radius

3. **`src/components/filters/PriceRangeSlider.tsx`**
   - Строка 220: `rounded-full` → проверить токены

4. **`src/components/checkbox/Checkbox.stories.tsx`**
   - Строка 260: `rounded` → токен radius

### 2.4 Typography размеры

**Примеры:**

1. **`src/components/SectionBuilder.stories.tsx`**
   - Строка 147: `text-4xl` → токен типографии
   - Строка 195: `text-3xl` → токен типографии
   - Строка 225-250: `text-2xl` → токен типографии
   - Строка 279-285: `text-3xl` → токен типографии

2. **`src/components/filters/PriceRangeSlider.tsx`**
   - Строка 151: `text-sm` → может быть OK, но лучше проверить токены

---

## 🎯 Категория 3: Прямое использование CSS переменных в классах

### 3.1 HSL переменные в Tailwind классах

**Примеры:**

1. **`src/components/input/input-variants.ts`**
   - Строка 16: `text-[hsl(var(--foreground))]` → должно использовать токен

2. **`src/components/input/Input.tsx`**
   - Строка 78: `text-[hsl(var(--muted-foreground))]` → токен
   - Строка 97: `text-[hsl(var(--muted-foreground))]` → токен

3. **`src/components/dropdown/dropdown-variants.ts`**
   - Строка 33: `ring-[hsl(var(--ring))]` → токен
   - Строка 51: `border-[hsl(var(--border))]`, `text-[hsl(var(--popover-foreground))]` → токены
   - Строка 55-56: `bg-[hsl(var(--popover))]`, `bg-[hsl(var(--muted))]` → токены
   - Строка 80-81: `bg-[hsl(var(--accent))]`, `text-[hsl(var(--accent-foreground))]` → токены
   - Строка 90: `bg-[hsl(var(--accent))]`, `text-[hsl(var(--accent-foreground))]` → токены

4. **`src/components/textarea/Textarea.tsx`**
   - Строка 90: `text-[hsl(var(--muted-foreground))]` → токен
   - Строка 91: `text-[hsl(var(--destructive))]` → токен

5. **`src/components/select/select-variants.ts`**
   - Строка 37: `ring-[hsl(var(--ring))]` → токен
   - Строка 55: `border-[hsl(var(--border))]`, `bg-[hsl(var(--popover))]`, `text-[hsl(var(--popover-foreground))]` → токены
   - Строка 76: `bg-[hsl(var(--accent))]`, `text-[hsl(var(--accent-foreground))]` → токены
   - Строка 86: `bg-[hsl(var(--accent))]`, `text-[hsl(var(--accent-foreground))]` → токены

6. **`src/components/input/Input.stories.tsx`**
   - Строка 201: `text-[hsl(var(--destructive))]` → токен
   - Строка 205: `text-[hsl(var(--semantic-success))]` → токен

7. **`src/components/textarea/Textarea.stories.tsx`**
   - Строка 207: `text-[hsl(var(--destructive))]` → токен
   - Строка 218: `text-[hsl(var(--semantic-success))]` → токен

### 3.2 Inline стили с CSS переменными

**Примеры:**

1. **`src/components/SectionBuilder.tsx`**
   - Строки 52-57: Используются CSS переменные напрямую в объекте

   ```typescript
   const surfaceVarMap: Record<string, string> = {
     base: "hsl(var(--surface-base))",
     elevated1: "hsl(var(--surface-elevated1))",
     // ...
   };
   ```

   **Проблема:** Хотя это технически правильно, лучше использовать токены из системы.

2. **`src/components/primitives/BrandShowcase.stories.tsx`**
   - Строки 297, 309: `backgroundColor: hsl(var(--brand-...))` → должно использовать токены

---

## 📐 Категория 4: Композитные классы с числовыми значениями

### 4.1 Комбинации классов

**Примеры:**

1. **`src/components/primitives/Link.tsx`**
   - Строки 27-31: Используются числовые классы в вариантах

   ```typescript
   xs: "h-7 text-xs px-xs py-xs",  // h-7, text-xs - числовые
   sm: "h-8 text-xs px-sm py-xs",  // h-8, text-xs - числовые
   ```

   **Проблема:** `h-7`, `h-8`, `text-xs` должны использовать токены

2. **`src/components/filters/FilterSelect.tsx`**
   - Строка 24: `h-10` → токен размера
   - Строка 31: `h-4 w-4` → токены размера иконки

3. **`src/components/filters/FilterBar.tsx`**
   - Строка 210: `h-3 w-3` → токены размера иконки
   - Строка 214: `h-3 w-3` → токены размера иконки

---

## 🎨 Категория 5: Цветовые классы с числовыми значениями

### 5.1 Цвета с числовыми значениями

**Примеры:**

1. **`src/components/SectionBuilder.presets.tsx`**
   - Строка 371: `text-yellow-500` → должен использовать токен семантического цвета

2. **`src/components/SectionBuilder.stories.tsx`**
   - Строка 147: `from-primary/20 to-accent/20` → может быть OK, но проверить токены
   - Строка 194: `from-secondary/20 to-primary/20` → может быть OK

---

## 📊 Статистика

### По категориям:

- **Хардкоженные цвета:** ~5 файлов
- **Числовые spacing классы:** ~80+ файлов
- **Прямые CSS переменные:** ~15 файлов
- **Числовые размеры:** ~50+ файлов
- **Числовые radius:** ~10 файлов
- **Числовые typography:** ~20 файлов

### По типам файлов:

- **Компоненты (.tsx):** ~100+ файлов
- **Variants (.ts):** ~10 файлов
- **Stories (.stories.tsx):** ~30+ файлов
- **Tests (.test.tsx):** ~5 файлов

---

## 🔧 Рекомендации по исправлению

### 1. Создать утилиты для конвертации

Создать helper функции для конвертации токенов в Tailwind классы:

```typescript
// src/lib/token-utils.ts
import { spacing, semanticSpacing } from "@/tokens";

export function getSpacingClass(value: keyof typeof semanticSpacing): string {
  return `p-${value}`; // или соответствующее
}
```

### 2. Использовать токены напрямую

Вместо:

```typescript
className = "px-4 py-2";
```

Использовать:

```typescript
import { semanticSpacing } from "@/tokens";
className={`px-${semanticSpacing.md} py-${semanticSpacing.sm}`}
```

Или лучше через систему вариантов CVA.

### 3. Заменить прямые CSS переменные

Вместо:

```typescript
className = "text-[hsl(var(--foreground))]";
```

Использовать:

```typescript
import { TEXT_TOKENS } from "@/tokens/components/text";
// или через варианты компонента
```

### 4. Создать токены для размеров иконок

Создать систему токенов для размеров иконок:

```typescript
// src/tokens/components/icon.ts
export const ICON_SIZE_TOKENS = {
  xs: "h-3 w-3",
  sm: "h-4 w-4",
  md: "h-5 w-5",
  lg: "h-6 w-6",
  xl: "h-8 w-8",
} as const;
```

### 5. Обновить Storybook stories

Заменить хардкоженные цвета в stories на токены:

```typescript
import { colors } from "@/tokens";

parameters: {
  backgrounds: {
    values: [
      { name: "light", value: colors.surface.base },
      { name: "dark", value: colors.surface.base },
    ],
  },
}
```

---

## ✅ Приоритет исправления

### Высокий приоритет:

1. **Компоненты variants** (`*-variants.ts`) - основа системы
2. **Основные компоненты** (Input, Dropdown, Select, Textarea)
3. **Layout компоненты** (Box, Flex, Grid, Stack)

### Средний приоритет:

1. **Card компоненты** (EventCard, VenueCard)
2. **Filter компоненты** (SearchInput, PriceRangeSlider, DateRangePicker)
3. **Navigation компоненты** (Pagination, Breadcrumbs)

### Низкий приоритет:

1. **Storybook stories** - в основном для демонстрации
2. **Test файлы** - могут использовать хардкод для тестирования
3. **Legacy компоненты** - будут удалены

---

## 📝 Следующие шаги

1. ✅ Создать этот отчет
2. ⬜ Создать утилиты для работы с токенами
3. ⬜ Обновить систему токенов (добавить недостающие)
4. ⬜ Исправить variants файлы
5. ⬜ Исправить основные компоненты
6. ⬜ Исправить layout компоненты
7. ⬜ Обновить остальные компоненты
8. ⬜ Обновить Storybook stories
9. ⬜ Добавить линтер правила для предотвращения хардкода

---

**Статус:** ✅ Отчет создан  
**Дата обновления:** 2025-12-09
