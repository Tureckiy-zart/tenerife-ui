# NPM Token Verification Guide

**Date**: 2025-01-20  
**Purpose**: Verify NPM_TOKEN configuration in GitHub Secrets

## ✅ Что уже проверено автоматически

1. ✅ Workflow правильно ссылается на `NPM_TOKEN`
2. ✅ `package.json` имеет `publishConfig.access: "public"`
3. ✅ Package использует scoped name: `@tenerife.music/ui`
4. ✅ Semantic-release настроен с npm publish

## 🔍 Ручная проверка GitHub Secrets

### Шаг 1: Проверьте наличие секрета

1. Перейдите: https://github.com/Tureckiy-zart/tenerife-ui/settings/secrets/actions
2. Найдите `NPM_TOKEN` в списке секретов
3. Убедитесь, что имя точно `NPM_TOKEN` (регистр важен!)

### Шаг 2: Проверьте формат токена

Токен должен:

- Начинаться с `npm_` (например: `npm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`)
- Быть типа "Automation Token" (рекомендуется для CI/CD)
- Иметь права "Read and Publish"

## 🧪 Тестирование NPM_TOKEN

### Вариант 1: Запустить тестовый workflow (рекомендуется)

1. Перейдите в **Actions** → **Test NPM Token**
2. Нажмите **"Run workflow"** → **"Run workflow"**
3. Дождитесь завершения и проверьте результаты

Workflow проверит:

- ✅ Наличие NPM_TOKEN в secrets
- ✅ Аутентификацию в npm
- ✅ Доступ к scope `@tenerife.music`
- ✅ Dry-run semantic-release

### Вариант 2: Локальная проверка (если есть токен)

```bash
# Установите токен временно
export NPM_TOKEN=your_token_here

# Проверьте аутентификацию
npm whoami --registry=https://registry.npmjs.org/

# Проверьте доступ к scope
npm access ls-packages @tenerife.music
```

### Вариант 3: Запустить скрипт проверки

```bash
# Запустите скрипт проверки конфигурации
bash .cursor/scripts/verify_npm_token.sh
```

## 📋 Чеклист проверки

Перед первым релизом убедитесь:

- [ ] `NPM_TOKEN` существует в GitHub Secrets
- [ ] Имя секрета точно `NPM_TOKEN` (регистр важен)
- [ ] Токен начинается с `npm_`
- [ ] Токен имеет права "Read and Publish"
- [ ] У вас есть доступ к scope `@tenerife.music` (или организация создана)
- [ ] Тестовый workflow прошел успешно

## 🚀 После проверки

Если все проверки пройдены:

1. Сделайте conventional commit:

   ```bash
   git commit -m "feat: test npm token configuration"
   ```

2. Запушьте в main:

   ```bash
   git push origin main
   ```

3. Мониторьте release workflow в Actions

4. Проверьте, что:
   - Package опубликован в npm
   - GitHub Release создан
   - CHANGELOG.md обновлен

## ⚠️ Возможные проблемы

### Проблема: "NPM_TOKEN not found"

**Решение:**

- Проверьте, что секрет добавлен в GitHub Secrets
- Убедитесь, что имя точно `NPM_TOKEN` (регистр важен)

### Проблема: "Unauthorized" при публикации

**Решение:**

- Проверьте, что токен не истек
- Убедитесь, что токен имеет права "Publish"
- Проверьте доступ к scope `@tenerife.music`

### Проблема: "Cannot publish to scope"

**Решение:**

- Создайте npm organization: https://www.npmjs.com/org/create
- Или используйте личный scope (например, `@tureckiy-zart/ui`)
- Или опубликуйте как unscoped package

## 📚 Дополнительные ресурсы

- Полная документация: `docs/reports/SEMVER_NPM_VALIDATION.md`
- Workflow конфигурация: `.github/workflows/release.yml`
- Release конфигурация: `release.config.cjs`
